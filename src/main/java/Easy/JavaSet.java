package Easy;

public class JavaSet {

    public static void main(String[] args) {
        // Rest of the code on HR's end.
        // First solution was string[], sets don't compare what's inside for string[] they just see memory references.
        // Second it's i < n, is the format. when i = n it stops accounting for the first equaling '0'

//        Set<String> newSet = new HashSet<>();
//        for (int i = 0; i < t; i++) {
//            String input = pair_left[i] + " " + pair_right[i];
//            newSet.add(input);
//
//            System.out.println(newSet.size());
//        }
    }
}
